<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="@android:style/Theme">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:actionBarStyle">@style/myActionBarStyle</item>
    </style>
          
    <style name="myActionBarStyle" parent="@android:style/Widget.ActionBar" >  
        <item name="android:background">@color/bg_blue</item> 
        <item name="android:titleTextStyle">@style/MyAlertDialogStyle</item>
    </style>  
    	        
    <style name="MyWindowStyle">
        <item name="android:background">@color/bg_black</item>
    </style>
    
    <style name="MySeekBarStyle">
        <item name="android:maxHeight">5.0dp</item>
        <item name="android:minHeight">5.0dp</item>
        <item name="android:progressDrawable">@drawable/seekbar_style</item>
    </style>
    
    <style name="MyAlertDialogStyle" parent="android:style/Theme.Dialog">
        <item name="android:background">@color/bg_blue</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:textColor">@color/textColorWhite</item>
    	<item name="android:textSize">16sp</item>
    	<item name="android:gravity">center</item>
    	
    </style>
    
</resources>
